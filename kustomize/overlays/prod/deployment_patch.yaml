apiVersion: apps/v1
kind: Deployment
metadata:
  name: csw-deployment
spec:
  template:
    spec:
      containers:
        - name: csw
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: csw-env-prod
